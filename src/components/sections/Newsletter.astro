---
// No necesitamos props para este componente
---

<section class="py-16 bg-amber-50">
  <div class="container mx-auto px-4">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl font-bold mb-4">Únete a Nuestra Comunidad</h2>
      <p class="text-gray-600 mb-8">
        Suscríbete para recibir las últimas tendencias en decoración, ofertas exclusivas y consejos de diseño de interiores.
      </p>

      <form class="flex flex-col sm:flex-row gap-4 max-w-xl mx-auto">
        <input
          type="email"
          placeholder="Tu correo electrónico"
          class="flex-1 px-4 py-3 rounded-md border border-gray-300 focus:ring-2 focus:ring-amber-500 focus:border-amber-500"
          required
        />
        <button
          type="submit"
          class="bg-amber-600 hover:bg-amber-700 text-white px-6 py-3 rounded-md transition-colors duration-300 whitespace-nowrap"
        >
          Suscribirse
        </button>
      </form>

      <p class="text-sm text-gray-500 mt-4">
        Al suscribirte, aceptas recibir correos de marketing. Puedes darte de baja en cualquier momento.
      </p>
    </div>
  </div>
</section>

<script>
  // Manejar el envío del formulario
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.querySelector('form');
    
    if (form) {
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        
        const emailInput = form.querySelector('input[type="email"]') as HTMLInputElement;
        const email = emailInput.value.trim();
        
        try {
          // Aquí irían las llamadas a tu API para guardar el email
          console.log('Email suscrito:', email);
          
          // Mostrar mensaje de éxito
          alert('¡Gracias por suscribirte! Recibirás un correo de confirmación pronto.');
          
          // Limpiar el formulario
          emailInput.value = '';
        } catch (error) {
          console.error('Error al suscribirse:', error);
          alert('Hubo un error al procesar tu suscripción. Por favor, intenta de nuevo.');
        }
      });
    }
  });
</script>

